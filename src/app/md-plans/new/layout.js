export const metadata = {
    title: "New MD Plan | Limbus Company Team Building Hub",
    description: "Create a new md plan"
};

export default function NewMdPlanLayout({ children }) {
  return children;
}
