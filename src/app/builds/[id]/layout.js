export const metadata = {
    title: "Team Build | Limbus Company Team Building Hub",
    description: "View a team build"
};

export default function BuildLayout({ children }) {
  return <>{children}</>;
}
