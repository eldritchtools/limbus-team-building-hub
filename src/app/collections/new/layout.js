export const metadata = {
    title: "New Collection | Limbus Company Team Building Hub",
    description: "Create a new collection"
};

export default function NewCollectionLayout({ children }) {
  return children;
}
