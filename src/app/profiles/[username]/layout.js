export const metadata = {
    title: "Profile | Limbus Company Team Building Hub",
    description: "View a user's builds"
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}