export const metadata = {
    title: "Forgot Password | Limbus Company Team Building Hub",
    description: "Forgot password"
};

export default function ForgotPasswordLayout({ children }) {
  return <>{children}</>;
}
