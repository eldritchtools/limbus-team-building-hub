export const metadata = {
    title: "Edit MD Plan | Limbus Company Mirror Dungeon Resource",
    description: "Edit a md plan"
};

export default function EditMdPlanLayout({ children }) {
  return children;
}
