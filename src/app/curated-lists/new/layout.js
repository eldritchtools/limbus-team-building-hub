export const metadata = {
    title: "New Curated List | Limbus Company Team Building Hub",
    description: "Create a new curated list"
};

export default function NewCuratedListLayout({ children }) {
  return children;
}
