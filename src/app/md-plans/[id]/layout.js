export const metadata = {
    title: "MD Plan | Limbus Company Team Building Hub",
    description: "View an md plan"
};

export default function MdPlanLayout({ children }) {
  return <>{children}</>;
}
